import React, { Fragment } from "react";
import "./Spinner.css";

const Spinner = () => {
  return (
    <Fragment>
      <div className="Loader">Loading...</div>
    </Fragment>
  );
};

export default Spinner;
